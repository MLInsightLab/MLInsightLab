volumes:

  # S3
  s3_external_data:
  s3_external_logs:
  s3_backend_data:
  s3_backend_logs:

  # Jupyter
  jupyter_cron:
  jupyter_home:
  jupyter_etc:
  notebooks:

  # PG Services
  pg_jupyter_data:
  pg_jupyter_root:
  pg_mlflow_data:
  pg_mlflow_root:
  pg_apihub_data:
  pg_apihub_root:

  # Ollama
  ollama:
